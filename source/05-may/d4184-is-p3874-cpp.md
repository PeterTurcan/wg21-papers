---
title: "Info: Is P3874R1 C++?"
document: P4184R0
date: 2026-04-11
reply-to:
  - "Claude Opus 4.6"
  - "Vinnie Falco <vinnie.falco@gmail.com>"
audience: WG21
---

## Abstract

An AI determines if [P3874R1](https://wg21.link/p3874r1) is C++ by applying the checklist from [P4183](https://wg21.link/p4183) "Tool: Is This C++?", which distills twenty-one design principles from Bjarne Stroustrup, *The Design and Evolution of C++* (Addison-Wesley, 1994).

---

## Revision History

### R0: April 2026 (post-Croydon mailing)

* Initial version.

---

## 1. Disclosure

The author provides information and serves at the pleasure of the committee.

This paper, and every part of the process - building, writing, editing - was produced with the help of a large language model.

This paper asks for nothing.

Everything below this line is the output of a large language model. The better the model, the better the results. AI output must always be verified by a human.

\newpage

# Is P3874R1 C++?

* **Document:** P3874R1
* **Subject:** "Should C++ be a memory-safe language?"
* **Authors:** Jon Bauman, Timur Doumler, Nevin Liber, Ryan McDougall, Pablo Halpern, Jeff Garland, Jonathan M&uuml;ller
* **Date:** Feb 23, 2026
* **Model:** claude-opus-4-0-20250514

---

**1. Does the subject avoid imposing cost on programs that do not use it?**

- Defining C++ as a memory-safe language: "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset." - supports-yes [verified]
- A memory-safe C++ is about new code: "The value of a memory-safe subset is focused on new code." - supports-yes [verified]
- Motivation: "No amount of precondition assertions, safety profiles, or standard library hardening will suffice to make C++ a memory-safe language unless paired with superset features that provide a subset that is both useful and systematically free of undefined behavior." - supports-no [verified: reaching the goal requires broad superset additions, a cross-cutting evolution burden not limited to opt-in code]
- Lifetime safety: "Bounds safety, for instance, entails a performance cost, but is straightforward to enforce." - supports-no [verified: acknowledges runtime cost without limiting that cost to opt-in code]
- Rust and Circle: "It does not commit to a particular design." - supports-no [verified: without a design commitment, zero-overhead cannot be assured]

**Answer:** No. The paper acknowledges bounds safety "entails a performance cost" without establishing that this cost is borne only by code that opts into the safe subset, and the goal requires superset features whose cost to non-users is unspecified.

---

**2. Is the subject affordable on ordinary hardware?**

- Lifetime safety: "Bounds safety, for instance, entails a performance cost, but is straightforward to enforce." - supports-no [overturned: acknowledging a performance cost does not say the cost is unaffordable on ordinary hardware; bounds checking is routine on every platform]
- Lifetime safety: Borrow checking described as "purely a compile-time phenomenon and based on local analysis." - supports-yes [verified: compile-time-only checking has no hardware cost]
- Appendix: "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available." - supports-yes [verified]
- What is a memory-safe language?: "UB is a necessary feature of systems languages, which demand maximum performance and low-level hardware access." - supports-yes [verified: the paper ties its proposal to systems languages that run on common hardware]

**Answer:** Yes. The paper's proposed mechanisms (borrow checking is compile-time only, bounds checking is routine, escape hatches preserve full performance) are all affordable on ordinary hardware, and the paper explicitly targets the systems language domain that runs on common hardware.

---

**3. Does the subject leave no room for a lower-level language below it?**

- Defining C++ as a memory-safe language: "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset." - supports-no [verified: institutionalizes a UB-permitting outer layer around the safe core]
- Appendix: "In non-systems languages, performance is compromised to achieve memory safety and in the memory-safe subset of systems languages, expressiveness is compromised." - supports-no [verified: the safe subset is explicitly less expressive than the full language]
- Appendix: "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available." - supports-no [verified: preserves a more-capable path beneath the safe surface, structurally creating room below]
- Defining: "it would be technically possible to define a subset excluding all runtime-UB-triggering operations, a subset that does not allow pointer dereference or arithmetic would not be useful. This is why it is necessary to add new features that replace those which must be restricted" - supports-no [verified: the safe subset cannot do everything the full language does without new features that do not yet exist]

**Answer:** No. The paper's subset-of-superset strategy explicitly creates a restricted safe subset that is less expressive than the full language, structurally creating room for a lower level below the safe surface.

---

**4. Does the subject preserve the static type system without implicit violations?**

- Defining: "The subset is syntactically explicit and well-defined; no undefined behavior can be exhibited within it." - supports-yes [verified]
- Appendix: Sound APIs are "analogous to how type-safe APIs can be implemented in terms of properly encapsulated type-unsafe constructs in C++, such as the example of std::variant being implemented in terms of a union." - supports-yes [verified]
- Appendix: "safety conditions can depend on the maintenance of safety invariants in program state that can be invalidated by code in the memory-safe subset." - supports-no [verified: safe code can invalidate invariants that unsafe code depends on, meaning the type/safety system has implicit failure modes across the boundary]
- Defining: "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset." - supports-no [verified: UB-capable code can reach into the safe subset, so the overall language does not prevent implicit violations]

**Answer:** No. The paper acknowledges that "safety conditions can depend on the maintenance of safety invariants in program state that can be invalidated by code in the memory-safe subset," meaning the boundary between safe and unsafe code allows implicit invariant violations.

---

**5. Are unsafe operations explicit and visible?**

- Why is Rust considered a memory-safe language?: Guarantees are "systematic and syntactically explicit." - supports-yes [verified]
- Appendix: "One of the major benefits of explicit unsafe block syntax is auditability. There is a clear indication of all code regions where upholding safety conditions are the responsibility of the programmer." - supports-yes [verified]
- Appendix: "all operations that ultimately trigger UB are disallowed in the memory-safe context" and UB follows from "failure to uphold the safety conditions on which code in a syntactic unsafe context depends." - supports-yes [verified]
- Appendix: "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available." - supports-yes [verified]

**Answer:** Yes. The paper's central design principle is syntactically explicit unsafe regions with "a clear indication of all code regions where upholding safety conditions are the responsibility of the programmer."

---

**6. Does the subject treat user-defined types as well as built-in types?**

**Answer:** No. No evidence of compliance in the subject.

---

**7. Does the subject prefer compile-time checking over run-time checking?**

- Lifetime safety: Borrow checking "is purely a compile-time phenomenon and based on local analysis." - supports-yes [verified]
- Defining: Guarantees "are locally enforced both during compilation and at runtime." - supports-no [verified: the definition explicitly includes runtime enforcement as coequal with compile-time]
- Appendix: Vector insert uses `assert!(index <= self.len)` to avoid UB via termination. - supports-no [verified: runtime assertion is presented as a normal soundness mechanism, not a fallback]

**Answer:** No. The paper defines memory-safety guarantees as "locally enforced both during compilation and at runtime," treating runtime checking as a coequal enforcement mechanism rather than preferring compile-time checking.

---

**8. Does the subject refrain from forcing the programmer into a single style?**

- Should C++ become?: Presents three options and states "While some members may support (1), the current path of the committee seems to be (2)." - supports-yes [verified]
- Appendix: Escape hatch preserves "both full performance and expressiveness" alongside the safe subset. - supports-yes [verified]
- Motivation: "This proposal does not seek to make the perfect the enemy of the good" and existing safety work "should continue." - supports-yes [verified]
- Defining: "Fundamentally, the subset is restrictive for the purpose of providing a guarantee." - supports-no [verified: within the safe subset, programmers are constrained]
- Appendix: "In the memory-safe subset of systems languages, expressiveness is compromised." - supports-no [verified: the memory-safe mode trades away expressiveness]

**Answer:** No. The paper states "Fundamentally, the subset is restrictive for the purpose of providing a guarantee of the memory safety property," and acknowledges "expressiveness is compromised" in the safe subset, meaning programmers who enter it are forced into a constrained style where UB-triggering operations are disallowed.

---

**9. Does the subject allow useful features rather than preventing every misuse?**

- Defining: "a subset that does not allow pointer dereference or arithmetic would not be useful. This is why it is necessary to add new features that replace those which must be restricted." - supports-yes [verified]
- Appendix: Sound `get()` wraps memory-unsafe `get_unchecked()`. - supports-yes [verified]
- Appendix: "all operations that ultimately trigger UB are disallowed in the memory-safe context." - supports-no [verified: in the safe context, UB-triggering operations are categorically disallowed]

**Answer:** No. The paper states "all operations that ultimately trigger UB are disallowed in the memory-safe context," which is a design that prevents misuse by categorical prohibition rather than allowing useful features that carry risk.

---

**10. Can the subject be combined with other programming styles?**

- Defining: "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset." - supports-yes [verified]
- Motivation: Build "islands of memory safety without having to switch languages entirely." - supports-yes [verified]
- Why is Rust considered?: Programs depend on "unsafe Rust code in the standard library as well as system libraries written in C or C++." - supports-yes [verified]

**Answer:** Yes. The subset-of-superset strategy explicitly enables combining safe and unsafe code in one program, and the paper frames "islands of memory safety" as coexisting with legacy C++.

---

**11. Is the subject driven by real problems, not theory alone?**

- Motivation: "numerous recommendations from industry, academia and government to prefer 'memory-safe languages'" and C++ is "critical infrastructure." - supports-yes [verified]
- What is a memory-safe language?: Weird machines, exploitation, security vulnerabilities. - supports-yes [verified]
- Appendix: "advances in theory, developed in response to C++'s three decades of global success and the persistent challenges posed by memory-safety bugs." - supports-yes [verified]
- Rust and Circle: "It does not commit to a particular design." - supports-no [overturned: declining to pick a specific design at the goal-setting stage does not mean the motivation is theoretical; the motivation section is grounded in deployment reality]

**Answer:** Yes. The paper is grounded in government mandates, industry pressure, security vulnerability data, and "three decades of global success and the persistent challenges posed by memory-safety bugs."

---

**12. Does the subject provide a transition path from existing practice?**

- Motivation: "islands of memory safety without having to switch languages entirely, which is often impractical or prohibitively expensive." - supports-yes [verified]
- A memory-safe C++ is about new code: "The value of a memory-safe subset is focused on new code" and existing mitigations "should continue." - supports-yes [verified]
- Standard library: "The reality of decades of legacy C++ existing is unavoidable, but it does not preclude the value of adding memory-safety for new code." - supports-yes [verified]

**Answer:** Yes. The paper explicitly targets new code while preserving existing code and existing safety efforts, providing "islands of memory safety" as a gradual transition rather than requiring wholesale rewrites.

---

**13. Is the subject useful now?**

- Defining: Subset must be "useful enough for the vast majority of programming tasks." - supports-yes [overturned: this is an aspiration for a future design, not a statement about current utility]
- Standard library: "Perhaps the greatest concern with regards to a memory-safe C++ is that it will require an entire new standard library before it is useful." - supports-no [verified]
- Standard library: Current library use from the subset "would require frequent escapes... it would be considerably unergonomic." - supports-no [verified]

**Answer:** No. The paper itself flags that a memory-safe C++ "will require an entire new standard library before it is useful" and that current library usage from the safe subset "would be considerably unergonomic."

---

**14. Can the subject be composed with separately developed parts?**

- Motivation: Organizations must "maintain large existing C++ codebases and integrate new components." - supports-yes [verified]
- Defining: Outside code "can still directly access all code within the well-defined subset." - supports-yes [verified]
- Appendix: "memory-unsafe code can be effectively encapsulated and used from the memory-safe subset without risk of triggering UB." - supports-yes [verified]

**Answer:** Yes. The subset-of-superset strategy explicitly enables composition between safe and unsafe components across abstraction boundaries.

---

**15. Is the subject a language facility, not a complete system?**

- Defining: Proposes a "compiler-enforced language subset which is free of UB" as a language-level mechanism. - supports-yes [verified]
- Defining: "C++ already has a subset which is free of UB in the form of constexpr." - supports-yes [verified]
- Standard library: "Perhaps the greatest concern with regards to a memory-safe C++ is that it will require an entire new standard library before it is useful." - supports-no [verified: the paper ties usefulness to a massive library effort, pushing beyond a pure language facility]

**Answer:** No. The paper acknowledges the proposal's usefulness depends on "an entire new standard library," making it a system-level undertaking rather than a self-contained language facility.

---

**16. Does the subject prefer general mechanisms over built-in special-purpose features?**

- Appendix: Sound APIs are "analogous to how type-safe APIs can be implemented in terms of properly encapsulated type-unsafe constructs in C++, such as the example of std::variant being implemented in terms of a union." - supports-yes [verified]
- Lifetime safety: "it seems clear that if C++ is to achieve memory-safety, some significant new feature will be required." - supports-no [verified: anticipates a major new mechanism for lifetimes, which reads as a special-purpose safety feature]

**Answer:** No. The paper anticipates that "some significant new feature will be required" for lifetime safety, pointing toward a special-purpose mechanism rather than preferring general abstraction improvements.

---

**17. Does the subject support deterministic resource management?**

**Answer:** No. No evidence of compliance in the subject.

---

**18. Does the subject provide manual control when automation is insufficient?**

- Appendix: "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available, but the cost is that avoiding UB relies on the programmer upholding guarantees that cannot be checked by the compiler." - supports-yes [verified]
- Appendix: "the use of the unsafe keyword enables operations that the compiler cannot check and puts the responsibility for avoiding UB on the programmer." - supports-yes [verified]
- Standard library: "using the existing standard library would require frequent escapes." - supports-yes [verified]

**Answer:** Yes. The paper explicitly provides an escape hatch where "the compiler cannot check and puts the responsibility for avoiding UB on the programmer," which is manual control when automation is insufficient.

---

**19. Can the programmer say what they mean in the language itself?**

- Defining: "The subset is syntactically explicit and well-defined." - supports-yes [verified]
- Why is Rust considered?: Guarantees are "systematic and syntactically explicit." - supports-yes [verified]
- Appendix: Safe intent is partly carried by `unsafe` regions and documented safety obligations, not only by safe-subset syntax. - supports-no [verified: safety conditions are documented in comments and documentation, not expressible in the language itself]

**Answer:** No. The paper acknowledges that safety conditions on unsafe code are upheld by "the programmer upholding guarantees that cannot be checked by the compiler" - these guarantees live in documentation and comments, not expressible in the language itself.

---

**20. Do application concepts map directly to language constructs?**

**Answer:** No. No evidence of compliance in the subject.

---

**21. Does this make the safe thing easy and the unsafe thing possible?**

- Appendix: "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available." - supports-yes [verified: unsafe remains possible]
- Appendix: Memory-safe subsets offer "sufficient expressiveness and performance for the vast majority of uses." - supports-yes [verified]
- Standard library: "it would be considerably unergonomic" to use the current standard library from the safe subset. - supports-no [verified: the safe thing is not easy without a new standard library]
- Appendix: "Ultimately, there is no free lunch and achieving both memory-safety and maximum performance depends on human diligence." - supports-no [verified: safe + performant is acknowledged as hard, not easy]

**Answer:** No. The paper concedes that using the safe subset with today's standard library "would be considerably unergonomic" and that "there is no free lunch," meaning the safe thing is not made easy.

---

**Score:** 7 / 21

**Verdict:** **This is not even close to C++.**
