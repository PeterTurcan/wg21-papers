---
title: "Is P3874R1 C++?"
document: P4184R0
date: 2026-05-01
intent: info
audience: WG21
reply-to:
  - "Claude Opus 4.6"
  - "Vinnie Falco <vinnie.falco@gmail.com>"
---

## Abstract

An AI determines if [P3874R1](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3874r1.pdf) is C++ by applying the checklist from [P4183R0](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p4183r0.pdf) "Tool: Is This C++? Find Out With This Tool", which distills twenty-four design principles from Bjarne Stroustrup, *The Design and Evolution of C++* (Addison-Wesley, 1994).

---

## Revision History

### R0: May 2026 (pre-Brno mailing)

* Initial version.

---

## 1. Disclosure

The author provides information and serves at the pleasure of the committee.

This paper, and every part of the process - building, writing, editing - was produced with the help of a large language model.

This paper asks for nothing.

Everything below this line is the output of a large language model. The better the model, the better the results. AI output must always be verified by a human.

\newpage

## Is P3874R1 C++?

* **Document:** P3874R1
* **Subject:** "Should C++ be a memory-safe language?"
* **Authors:** Jon Bauman, Timur Doumler, Nevin Liber, Ryan McDougall, Pablo Halpern, Jeff Garland, Jonathan M&uuml;ller
* **Date:** Feb 23, 2026
* **Model:** claude-opus-4-0-20250514

---

**1. Does the subject avoid imposing cost on programs that do not use it?**

**Answer:** No. The paper acknowledges bounds safety "entails a performance cost" without establishing that this cost is borne only by code that opts into the safe subset, and the goal requires superset features whose cost to non-users is unspecified.

- **Defining C++ as a memory-safe language:** "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset."
  - Tag: supports-yes [verified]
  - Reasoning: Outside code is not forced to change.

- **A memory-safe C++ is about new code:** "The value of a memory-safe subset is focused on new code."
  - Tag: supports-yes [verified]
  - Reasoning: Existing code that does not use the feature is not affected.

- **Motivation:** "No amount of precondition assertions, safety profiles, or standard library hardening will suffice to make C++ a memory-safe language unless paired with superset features that provide a subset that is both useful and systematically free of undefined behavior."
  - Tag: supports-no [verified]
  - Reasoning: Reaching the goal requires broad superset additions, a cross-cutting evolution burden not limited to opt-in code.

- **Lifetime safety is the primary challenge:** "Bounds safety, for instance, entails a performance cost, but is straightforward to enforce."
  - Tag: supports-no [verified]
  - Reasoning: Acknowledges runtime cost without limiting that cost to opt-in code.

- **Rust and Circle as a theoretical design:** "It does not commit to a particular design."
  - Tag: supports-no [verified]
  - Reasoning: Without a design commitment, zero-overhead cannot be assured.

---

**2. Is the subject affordable on ordinary hardware?**

**Answer:** Yes. The paper's proposed mechanisms (borrow checking is compile-time only, bounds checking is routine, escape hatches preserve full performance) are all affordable on ordinary hardware, and the paper explicitly targets the systems language domain that runs on common hardware.

- **Lifetime safety is the primary challenge:** "Bounds safety, for instance, entails a performance cost, but is straightforward to enforce."
  - Tag: supports-no [overturned]
  - Reasoning: Acknowledging a performance cost does not say the cost is unaffordable on ordinary hardware; bounds checking is routine on every platform.

- **Lifetime safety is the primary challenge:** Borrow checking described as "purely a compile-time phenomenon and based on local analysis."
  - Tag: supports-yes [verified]
  - Reasoning: Compile-time-only checking has no hardware cost.

- **Appendix: Encapsulating Unsafety:** "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available."
  - Tag: supports-yes [verified]
  - Reasoning: Escape hatches preserve full performance on any hardware.

- **What is a memory-safe language?:** "UB is a necessary feature of systems languages, which demand maximum performance and low-level hardware access."
  - Tag: supports-yes [verified]
  - Reasoning: The paper ties its proposal to systems languages that run on common hardware.

---

**3. Does the subject leave no room for a lower-level language below it?**

**Answer:** No. The paper's subset-of-superset strategy explicitly creates a restricted safe subset that is less expressive than the full language, structurally creating room for a lower level below the safe surface.

- **Defining C++ as a memory-safe language:** "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset."
  - Tag: supports-no [verified]
  - Reasoning: Institutionalizes a UB-permitting outer layer around the safe core.

- **Appendix: Encapsulating Unsafety:** "In non-systems languages, performance is compromised to achieve memory safety and in the memory-safe subset of systems languages, expressiveness is compromised."
  - Tag: supports-no [verified]
  - Reasoning: The safe subset is explicitly less expressive than the full language.

- **Appendix: Encapsulating Unsafety:** "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available."
  - Tag: supports-no [verified]
  - Reasoning: Preserves a more-capable path beneath the safe surface, structurally creating room below.

- **Defining C++ as a memory-safe language:** "it would be technically possible to define a subset excluding all runtime-UB-triggering operations, a subset that does not allow pointer dereference or arithmetic would not be useful. This is why it is necessary to add new features that replace those which must be restricted"
  - Tag: supports-no [verified]
  - Reasoning: The safe subset cannot do everything the full language does without new features that do not yet exist.

---

**4. Does the subject preserve the static type system without implicit violations?**

**Answer:** No. The paper acknowledges that "safety conditions can depend on the maintenance of safety invariants in program state that can be invalidated by code in the memory-safe subset," meaning the boundary between safe and unsafe code allows implicit invariant violations.

- **Defining C++ as a memory-safe language:** "The subset is syntactically explicit and well-defined; no undefined behavior can be exhibited within it."
  - Tag: supports-yes [verified]
  - Reasoning: Within the subset, the type/safety system holds.

- **Appendix: Encapsulating Unsafety:** Sound APIs are "analogous to how type-safe APIs can be implemented in terms of properly encapsulated type-unsafe constructs in C++, such as the example of std::variant being implemented in terms of a union."
  - Tag: supports-yes [verified]
  - Reasoning: Encapsulation pattern preserves type safety at the boundary.

- **Appendix: Encapsulating Unsafety:** "safety conditions can depend on the maintenance of safety invariants in program state that can be invalidated by code in the memory-safe subset."
  - Tag: supports-no [verified]
  - Reasoning: Safe code can invalidate invariants that unsafe code depends on, meaning the type/safety system has implicit failure modes across the boundary.

- **Defining C++ as a memory-safe language:** "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset."
  - Tag: supports-no [verified]
  - Reasoning: UB-capable code can reach into the safe subset, so the overall language does not prevent implicit violations.

---

**5. Are unsafe operations explicit and visible?**

**Answer:** Yes. The paper's central design principle is syntactically explicit unsafe regions with "a clear indication of all code regions where upholding safety conditions are the responsibility of the programmer."

- **Why is Rust considered a memory-safe language?:** Guarantees are "systematic and syntactically explicit."
  - Tag: supports-yes [verified]
  - Reasoning: Explicitness is a core design principle.

- **Appendix: Encapsulating Unsafety:** "One of the major benefits of explicit unsafe block syntax is auditability. There is a clear indication of all code regions where upholding safety conditions are the responsibility of the programmer."
  - Tag: supports-yes [verified]
  - Reasoning: Unsafe regions are syntactically marked and auditable.

- **Appendix: Encapsulating Unsafety:** "all operations that ultimately trigger UB are disallowed in the memory-safe context" and UB follows from "failure to uphold the safety conditions on which code in a syntactic unsafe context depends."
  - Tag: supports-yes [verified]
  - Reasoning: UB is confined to explicitly marked unsafe contexts.

- **Appendix: Encapsulating Unsafety:** "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available."
  - Tag: supports-yes [verified]
  - Reasoning: The escape hatch is explicit by design.

---

**6. Does the subject treat user-defined types as well as built-in types?**

**Answer:** N/A. P3874R1 is a direction paper about memory safety goals; it does not propose type-system facilities and has no bearing on the treatment of user-defined vs. built-in types.

---

**7. Does the subject prefer compile-time checking over run-time checking?**

**Answer:** No. The paper defines memory-safety guarantees as "locally enforced both during compilation and at runtime," treating runtime checking as a coequal enforcement mechanism rather than preferring compile-time checking.

- **Lifetime safety is the primary challenge:** Borrow checking "is purely a compile-time phenomenon and based on local analysis."
  - Tag: supports-yes [verified]
  - Reasoning: Lifetime checking is compile-time only.

- **Defining C++ as a memory-safe language:** Guarantees "are locally enforced both during compilation and at runtime."
  - Tag: supports-no [verified]
  - Reasoning: The definition explicitly includes runtime enforcement as coequal with compile-time.

- **Appendix: Encapsulating Unsafety:** "Vector insert uses `assert!(index <= self.len)` to avoid UB via termination."
  - Tag: supports-no [verified]
  - Reasoning: Runtime assertion is presented as a normal soundness mechanism, not a fallback.

---

**8. Does the subject refrain from forcing the programmer into a single style?**

**Answer:** No. The paper states "Fundamentally, the subset is restrictive for the purpose of providing a guarantee of the memory safety property," and acknowledges "expressiveness is compromised" in the safe subset, meaning programmers who enter it are forced into a constrained style where UB-triggering operations are disallowed.

- **Should C++ become a memory-safe language?:** Presents three options and states "While some members may support (1), the current path of the committee seems to be (2)."
  - Tag: supports-yes [verified]
  - Reasoning: Multiple paths are acknowledged at the committee level.

- **Appendix: Encapsulating Unsafety:** Escape hatch preserves "both full performance and expressiveness" alongside the safe subset.
  - Tag: supports-yes [verified]
  - Reasoning: Outside the subset, no style is forced.

- **Motivation:** "This proposal does not seek to make the perfect the enemy of the good" and existing safety work "should continue."
  - Tag: supports-yes [verified]
  - Reasoning: Existing approaches are not displaced.

- **Defining C++ as a memory-safe language:** "Fundamentally, the subset is restrictive for the purpose of providing a guarantee."
  - Tag: supports-no [verified]
  - Reasoning: Within the safe subset, programmers are constrained.

- **Appendix: Encapsulating Unsafety:** "In the memory-safe subset of systems languages, expressiveness is compromised."
  - Tag: supports-no [verified]
  - Reasoning: The memory-safe mode trades away expressiveness.

---

**9. Does the subject allow useful features rather than preventing every misuse?**

**Answer:** No. The paper states "all operations that ultimately trigger UB are disallowed in the memory-safe context," which is a design that prevents misuse by categorical prohibition rather than allowing useful features that carry risk.

- **Defining C++ as a memory-safe language:** "a subset that does not allow pointer dereference or arithmetic would not be useful. This is why it is necessary to add new features that replace those which must be restricted."
  - Tag: supports-yes [verified]
  - Reasoning: Replacement features are intended to restore usefulness.

- **Appendix: Encapsulating Unsafety:** "Sound `get()` wraps memory-unsafe `get_unchecked()`."
  - Tag: supports-yes [verified]
  - Reasoning: Safe wrappers provide useful access.

- **Appendix: Encapsulating Unsafety:** "all operations that ultimately trigger UB are disallowed in the memory-safe context."
  - Tag: supports-no [verified]
  - Reasoning: In the safe context, UB-triggering operations are categorically disallowed.

---

**10. Can the subject be combined with other programming styles?**

**Answer:** Yes. The subset-of-superset strategy explicitly enables combining safe and unsafe code in one program, and the paper frames "islands of memory safety" as coexisting with legacy C++.

- **Defining C++ as a memory-safe language:** "Code outside the subset may exhibit undefined behavior, but can still directly access all code within the well-defined subset."
  - Tag: supports-yes [verified]
  - Reasoning: Safe and unsafe code coexist in one program.

- **Motivation:** Build "islands of memory safety without having to switch languages entirely."
  - Tag: supports-yes [verified]
  - Reasoning: Gradual adoption enables combining styles.

- **Why is Rust considered a memory-safe language?:** Programs depend on "unsafe Rust code in the standard library as well as system libraries written in C or C++."
  - Tag: supports-yes [verified]
  - Reasoning: The model the paper endorses inherently combines safe and unsafe styles.

---

**11. Is the subject driven by real problems, not theory alone?**

**Answer:** Yes. The paper is grounded in government mandates, industry pressure, security vulnerability data, and "three decades of global success and the persistent challenges posed by memory-safety bugs."

- **Motivation:** "numerous recommendations from industry, academia and government to prefer 'memory-safe languages'" and C++ is "critical infrastructure."
  - Tag: supports-yes [verified]
  - Reasoning: Real-world pressure from multiple sectors drives the proposal.

- **What is a memory-safe language?:** "Weird machines, exploitation, security vulnerabilities."
  - Tag: supports-yes [verified]
  - Reasoning: Concrete security problems motivate the work.

- **Appendix: Encapsulating Unsafety:** "advances in theory, developed in response to C++'s three decades of global success and the persistent challenges posed by memory-safety bugs."
  - Tag: supports-yes [verified]
  - Reasoning: Theory was developed in response to real deployment challenges.

- **Rust and Circle as a theoretical design:** "It does not commit to a particular design."
  - Tag: supports-no [overturned]
  - Reasoning: Declining to pick a specific design at the goal-setting stage does not mean the motivation is theoretical; the motivation section is grounded in deployment reality.

---

**12. Does the subject provide a transition path from existing practice?**

**Answer:** Yes. The paper explicitly targets new code while preserving existing code and existing safety efforts, providing "islands of memory safety" as a gradual transition rather than requiring wholesale rewrites.

- **Motivation:** "islands of memory safety without having to switch languages entirely, which is often impractical or prohibitively expensive."
  - Tag: supports-yes [verified]
  - Reasoning: Gradual island-based adoption is a transition path.

- **A memory-safe C++ is about new code:** "The value of a memory-safe subset is focused on new code" and existing mitigations "should continue."
  - Tag: supports-yes [verified]
  - Reasoning: Existing code and practices are preserved.

- **What about the standard library?:** "The reality of decades of legacy C++ existing is unavoidable, but it does not preclude the value of adding memory-safety for new code."
  - Tag: supports-yes [verified]
  - Reasoning: Legacy compatibility is explicitly acknowledged.

---

**13. Is the subject useful now?**

**Answer:** No. The paper itself flags that a memory-safe C++ "will require an entire new standard library before it is useful" and that current library usage from the safe subset "would be considerably unergonomic."

- **Defining C++ as a memory-safe language:** Subset must be "useful enough for the vast majority of programming tasks."
  - Tag: supports-yes [overturned]
  - Reasoning: This is an aspiration for a future design, not a statement about current utility.

- **What about the standard library?:** "Perhaps the greatest concern with regards to a memory-safe C++ is that it will require an entire new standard library before it is useful."
  - Tag: supports-no [verified]
  - Reasoning: The paper concedes the feature is not useful without a new standard library.

- **What about the standard library?:** Current library use from the subset "would require frequent escapes... it would be considerably unergonomic."
  - Tag: supports-no [verified]
  - Reasoning: Current usage would be impractical.

---

**14. Can the subject be composed with separately developed parts?**

**Answer:** Yes. The subset-of-superset strategy explicitly enables composition between safe and unsafe components across abstraction boundaries.

- **Motivation:** Organizations must "maintain large existing C++ codebases and integrate new components."
  - Tag: supports-yes [verified]
  - Reasoning: Integration of separately developed components is a stated goal.

- **Defining C++ as a memory-safe language:** Outside code "can still directly access all code within the well-defined subset."
  - Tag: supports-yes [verified]
  - Reasoning: Cross-boundary access enables composition.

- **Appendix: Encapsulating Unsafety:** "memory-unsafe code can be effectively encapsulated and used from the memory-safe subset without risk of triggering UB."
  - Tag: supports-yes [verified]
  - Reasoning: Encapsulation enables separate development.

---

**15. Is the subject a language facility, not a complete system?**

**Answer:** No. The paper acknowledges the proposal's usefulness depends on "an entire new standard library," making it a system-level undertaking rather than a self-contained language facility.

- **Defining C++ as a memory-safe language:** Proposes a "compiler-enforced language subset which is free of UB" as a language-level mechanism.
  - Tag: supports-yes [verified]
  - Reasoning: The core proposal is a language-level mechanism.

- **Defining C++ as a memory-safe language:** "C++ already has a subset which is free of UB in the form of constexpr."
  - Tag: supports-yes [verified]
  - Reasoning: Analogizes to an existing language facility.

- **What about the standard library?:** "Perhaps the greatest concern with regards to a memory-safe C++ is that it will require an entire new standard library before it is useful."
  - Tag: supports-no [verified]
  - Reasoning: The paper ties usefulness to a massive library effort, pushing beyond a pure language facility.

---

**16. Does the subject prefer general mechanisms over built-in special-purpose features?**

**Answer:** No. The paper anticipates that "some significant new feature will be required" for lifetime safety, pointing toward a special-purpose mechanism rather than preferring general abstraction improvements.

- **Appendix: Encapsulating Unsafety:** Sound APIs are "analogous to how type-safe APIs can be implemented in terms of properly encapsulated type-unsafe constructs in C++, such as the example of std::variant being implemented in terms of a union."
  - Tag: supports-yes [verified]
  - Reasoning: The encapsulation pattern is a general mechanism.

- **Lifetime safety is the primary challenge:** "it seems clear that if C++ is to achieve memory-safety, some significant new feature will be required."
  - Tag: supports-no [verified]
  - Reasoning: Anticipates a major new mechanism for lifetimes, which reads as a special-purpose safety feature.

---

**17. Does the subject support deterministic resource management?**

**Answer:** N/A. P3874R1 is a direction paper about memory safety; it does not propose resource management facilities and has no bearing on RAII or deterministic destruction.

---

**18. Does the subject provide manual control when automation is insufficient?**

**Answer:** Yes. The paper explicitly provides an escape hatch where "the compiler cannot check and puts the responsibility for avoiding UB on the programmer," which is manual control when automation is insufficient.

- **Appendix: Encapsulating Unsafety:** "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available, but the cost is that avoiding UB relies on the programmer upholding guarantees that cannot be checked by the compiler."
  - Tag: supports-yes [verified]
  - Reasoning: The escape hatch is manual control by design.

- **Appendix: Encapsulating Unsafety:** "the use of the unsafe keyword enables operations that the compiler cannot check and puts the responsibility for avoiding UB on the programmer."
  - Tag: supports-yes [verified]
  - Reasoning: Explicitly transfers control to the programmer.

- **What about the standard library?:** "using the existing standard library would require frequent escapes."
  - Tag: supports-yes [verified]
  - Reasoning: Manual escape is available and expected to be used.

---

**19. Can the programmer say what they mean in the language itself?**

**Answer:** No. The paper acknowledges that safety conditions on unsafe code are upheld by "the programmer upholding guarantees that cannot be checked by the compiler" - these guarantees live in documentation and comments, not expressible in the language itself.

- **Defining C++ as a memory-safe language:** "The subset is syntactically explicit and well-defined."
  - Tag: supports-yes [verified]
  - Reasoning: Safe intent is expressible in the language syntax.

- **Why is Rust considered a memory-safe language?:** Guarantees are "systematic and syntactically explicit."
  - Tag: supports-yes [verified]
  - Reasoning: The safety model itself is expressed in language constructs.

- **Appendix: Encapsulating Unsafety:** "Safe intent is partly carried by `unsafe` regions and documented safety obligations, not only by safe-subset syntax."
  - Tag: supports-no [verified]
  - Reasoning: Safety conditions are documented in comments and documentation, not expressible in the language itself.

---

**20. Do application concepts map directly to language constructs?**

**Answer:** N/A. P3874R1 proposes a safety model, not application-domain abstractions; the question of mapping application concepts to language constructs has no bearing on the subject.

---

**21. Does the subject avoid depending on or encouraging preprocessor macros?**

**Answer:** N/A. P3874R1 does not propose or discuss preprocessor macros; the subject has no bearing on macro usage or module adoption.

---

**22. Can the subject's properties be verified by local inspection?**

**Answer:** Yes. The paper's borrow-checking proposal is described as "purely a compile-time phenomenon and based on local analysis," and the safe subset's guarantees "are locally enforced" - properties are verifiable at the point of use without non-local context.

- **Lifetime safety is the primary challenge:** Borrow checking "is purely a compile-time phenomenon and based on local analysis."
  - Tag: supports-yes [verified]
  - Reasoning: Local analysis is the explicit design for lifetime checking.

- **Defining C++ as a memory-safe language:** Guarantees "are locally enforced both during compilation and at runtime."
  - Tag: supports-yes [verified]
  - Reasoning: Local enforcement means properties are verifiable at the point of use.

---

**23. Does the subject integrate with existing C++ features rather than creating an isolated sub-language?**

**Answer:** No. The paper explicitly proposes a "safe subset" with categorically restricted operations - "all operations that ultimately trigger UB are disallowed in the memory-safe context" - and acknowledges "expressiveness is compromised," which is the definition of an isolated sub-language within the host language.

- **Defining C++ as a memory-safe language:** "Fundamentally, the subset is restrictive for the purpose of providing a guarantee."
  - Tag: supports-no [verified]
  - Reasoning: A restrictive subset that disallows operations available in the host language is an isolated sub-language by definition.

- **Appendix: Encapsulating Unsafety:** "In the memory-safe subset of systems languages, expressiveness is compromised."
  - Tag: supports-no [verified]
  - Reasoning: Compromised expressiveness means the subset cannot use all existing C++ features.

- **Appendix: Encapsulating Unsafety:** "all operations that ultimately trigger UB are disallowed in the memory-safe context."
  - Tag: supports-no [verified]
  - Reasoning: Categorical prohibition creates a boundary between the subset and the rest of C++.

- **Motivation:** Build "islands of memory safety without having to switch languages entirely."
  - Tag: supports-yes [verified]
  - Reasoning: The islands metaphor implies integration rather than complete isolation.

---

**24. Does this make the safe thing easy and the unsafe thing possible?**

**Answer:** No. The paper concedes that using the safe subset with today's standard library "would be considerably unergonomic" and that "there is no free lunch," meaning the safe thing is not made easy.

- **Appendix: Encapsulating Unsafety:** "By using the 'escape hatch' of explicit memory-unsafe operations, both full performance and expressiveness is available."
  - Tag: supports-yes [verified]
  - Reasoning: Unsafe remains possible.

- **Appendix: Encapsulating Unsafety:** Memory-safe subsets offer "sufficient expressiveness and performance for the vast majority of uses."
  - Tag: supports-yes [verified]
  - Reasoning: The safe path is intended to be sufficient.

- **What about the standard library?:** "it would be considerably unergonomic" to use the current standard library from the safe subset.
  - Tag: supports-no [verified]
  - Reasoning: The safe thing is not easy without a new standard library.

- **Appendix: Encapsulating Unsafety:** "Ultimately, there is no free lunch and achieving both memory-safety and maximum performance depends on human diligence."
  - Tag: supports-no [verified]
  - Reasoning: Safe + performant is acknowledged as hard, not easy.

---

**Score:** 8 / 20 applicable (4 skipped) = 40%

**Verdict:** **This is not even close to C++.**
